import { Button } from "@/components/ui/Button";
import type { StepProps } from "@/features/signup/types";

export default function Step{{stepNumber}}({ currentStep, totalSteps, onNext }: StepProps) {
  return (
    <div className="flex flex-col gap-6 px-6 py-8">
      <div className="flex flex-col gap-1">
        <p className="text-xs text-gray-400">{currentStep} / {totalSteps}</p>
        <h2 className="text-xl font-bold">스텝 {{stepNumber}} 제목</h2>
        <p className="text-sm text-gray-500">스텝 {{stepNumber}} 설명을 입력하세요.</p>
      </div>

      {/* 여기에 스텝 내용 추가 */}

      <div className="mt-auto">
        <Button size="full" onClick={onNext}>
          다음
        </Button>
      </div>
    </div>
  );
}
